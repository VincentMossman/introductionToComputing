with Ada.Text_IO;
with Ada.Integer_Text_IO;

procedure gay is

type lol is (a, b, c);

package lolio is new ada.Text_IO.Enumeration_IO (enum => lol);

type gayarray is array (integer range 1 .. 5) of lol;
   arraygay : gayarray;

begin

   Ada.Integer_Text_IO.put (199/100);

end;
   